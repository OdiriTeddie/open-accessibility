{
  "name": "@open-accessibility/core",
  "version": "0.1.0",
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "dev": "tsx src/index.ts",
    "lint": "eslint src",
    "test": "vitest run --passWithNoTests",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@open-accessibility/analyzer": "workspace:*",
    "@open-accessibility/browser": "workspace:*",
    "@open-accessibility/tree": "workspace:*"
  },
  "devDependencies": {
    "tsx": "^4.19.2"
  }
}
